import React from "react";
import { Typography } from '@material-ui/core';

const Portfolio = () => {
    return (
        <div>
        This is Portfolio
        </div>
    );
};

export default Portfolio;
